import { defineConfig } from '@umijs/max';

export default defineConfig({
  antd: {},
  model: {},
  initialState: {},
  request: {},
  routeProps: {},
  mfsu: false,
  layout: {
    title: '@umijs/max',
  },
  npmClient: 'pnpm',
});
